# Частина 3. Методологія та сирі результати

Цей файл відповідає на головне питання користувача: **«як саме ви перевірили це на Haiku, а не вигадали?»**

---

## Що було зроблено

Запущено оркестрований workflow з **33 агентів**:

- **30 прогонів моделі Claude Haiku 4.5** — 11 технік × 2 варіанти (слабкий/сильний), з них 2 техніки (`DS2`, `SE3`) прогнані по **3 рази** кожен варіант для перевірки стабільності формату.
- **3 дослідницькі агенти** — пошук і верифікація сайтів для навчання (результат — у [файлі 04](04-top-sajty-i-shpargalka.md)).

| Параметр | Значення |
|----------|----------|
| Модель для перевірки | `claude-haiku-4-5` |
| Кількість агентів | 33 |
| Прогонів моделі (DS+SWE) | 30 |
| Виклики інструментів | 65 |
| Токенів витрачено | ~506 000 |
| Час виконання | ~112 секунд |

> 📦 **Друга партія (+25 прогонів).** Окремий workflow додав 12 прогонів на **Sonnet 4.6** (порівняння) + 13 на Haiku (нові техніки й українські промпти). Результати й висновки — у [частині 5](05-rozshyrennya-haiku-vs-sonnet-uk.md).
>
> 📦 **Третя партія (+49 прогонів, усе через підагентів, без raw-API).** 40 прогонів на **надійність формату** (8 промптів × 5 семплів), 8 на **антипатерни**, 1 агент на **WebFetch-перевірку URL**. Так підагенти замінили перевірку через raw-API на N семплах. Результати — у [частині 6](06-stabilnist-ta-antypaterny.md).
>
> 📦 **Четверта партія (+14 прогонів, крос-модельна).** 7 прогонів на **Opus 4.8** + 7 на **Fable 5** (DS1 vague, DS4 «no markdown» ×5, SE6-пастка), щоб емпірично перевірити поведінкові твердження [Частини 8](08-vybir-modeli-haiku-opus-fable.md). Haiku-баейзлайни взято з першої партії.
>
> **Разом по курсу: 118 модельних прогонів** (Haiku 4.5 + Sonnet 4.6 + Opus 4.8 + Fable 5). Єдине джерело правди для цього числа — [`raw-outputs/runs-manifest.json`](raw-outputs/runs-manifest.json) (батч → скрипт → вихід → кількість), щоб не звіряти суму вручну по файлах.

> 🔍 **Перевірка актуальності (2026-08-21).** Окремим проходом курс звірено з живими доксами: механіка API Частини 8 — 11/11 підтверджено; ціни/ліміти трьох моделей — без змін; 10/10 URL живі. Виправлено: формулювання про CoT-`display` у Fable, облік посилань (9→10), додано нотатку про **Opus 5 / Sonnet 5**.

### Як проганявся кожен приклад
Кожному Haiku-агенту давали обгортку, що змушувала його відповідати **як звичайна LLM-модель** (без інструментів, без мета-коментарів), а всередині — тестований промпт у тегах `<user_message>`. Структурований вивід (`{response: string}`) гарантував, що ми зчитуємо саме відповідь моделі дослівно.

---

## Чому не «raw API»

Найчистіша перевірка — прямі HTTP-виклики до `api.anthropic.com` з `model: claude-haiku-4-5-...`. Це **не вдалося**, бо в системі **немає API-ключа** (`ANTHROPIC_API_KEY` не встановлено, пакета `anthropic` немає, CLI відсутній — перевірено на старті).

Тому перевірка йшла через **Haiku 4.5 у гарнесі агента**. Базова модель — справжня Haiku 4.5, тож її відповіді справжні. Але є нюанс ⬇️.

---

## Застереження

Будь чесним щодо меж цього методу:

1. **Тонке «забруднення» системним промптом.** Агент-гарнес має власний системний промпт. Ми просили модель «відповідай як звичайний асистент», але повної ізоляції, як у raw-API-виклику, немає.
2. **Доказ забруднення в даних.** Прогін `SE3 weak #1` протік рядок `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`. Цей рядок походить з **інструкцій гарнесу про git-коміти**, а не з «творчості Haiku». Я свідомо лишив його у [файлі 02](02-software-engineering.md#se3-few-shot-для-строгого-формату-conventional-commits) як живу ілюстрацію цього застереження.
3. **«Роль у system» апроксимована.** У `DS5` роль передана в тексті промпта, а не в окремому `system`-полі (через API було б трохи сильніше).
4. **Семплінг.** Температура/семплінг — за замовчуванням гарнесу. Тому 3-разові прогони `DS2`/`SE3` — це індикація стабільності, а не строгий статистичний експеримент.

**Висновок:** напрям і величина ефектів — справжні й відтворювані. Точні формулювання відповідей можуть трохи відрізнятися від raw-API. Для навчальних цілей це повноцінна емпірична перевірка; для наукової публікації — варто повторити через raw-API з фіксованими `temperature` і `seed`/кількома семплами.

---

## Зведена таблиця всіх прогонів

`Δ символів` = довжина(сильний) − довжина(слабкий). Знак показує напрям, а не «краще/гірше» (іноді сильний має бути довшим — як у CoT).

| ID | Домен | Техніка | Слабкий (симв.) | Сильний (симв.) | Що показав реальний прогін |
|----|-------|---------|----------------:|----------------:|----------------------------|
| DS1 | DS | Чіткість/конкретність | 1116 | 1125 | Слабкий → відмова й прохання дати дані; сильний → 5 конкретних порад + ризик leakage |
| DS2 | DS | Few-shot (формат) | 886 | **5** | 3 прогони: слабкий = 3 різні відповіді; сильний = `MIXED` ×3 однаково |
| DS3 | DS | Chain-of-Thought | 398 | **1806** | Слабкий → «No.» + 3 речення безконтекстного обґрунтування; сильний → розбір вартості FP/FN + рекомендація порога (різниця в якості міркувань, не в наявності) |
| DS4 | DS | JSON зі схемою | 326 | 118 | Сильний = коректний JSON з типами, але **порушив «no markdown»** (обгорнув у ```` ```json ````) |
| DS5 | DS | Роль/персона | 2429 | 1189 | Слабкий = енциклопедія 6 методів; сильний = прицільна порада (MICE, missing-indicator) |
| SE1 | SWE | Специфікація | 2949 | 615 | Слабкий = 3 підходи + DNS + регулярки; сильний = рівно та функція простими рядковими операціями |
| SE2 | SWE | XML-теги | 1399 | 1949 | Сильний знайшов **self-transfer bug + race condition**, яких слабкий не помітив |
| SE3 | SWE | Few-shot (commit) | 207 | **40** | 3 прогони: слабкий = 3 різні багаторядкові; сильний = однорядковий `type(scope):` ×3 |
| SE4 | SWE | Пошук вади (CoT) | 1035 | 1570 | Обидва знайшли mutable-default; CoT додав трасування 3 викликів + структуру |
| SE5 | SWE | Примус формату | 315 | **48** | Слабкий = JSON у ```` ``` ```` + пояснення; сильний = голий `{...}` (якір першого символу) |
| SE6 | SWE | Контроль галюцинацій | 1153 | 826 | **Обидва** правильно сказали «методу не існує» — Haiku 4.5 і так стійкий до пастки |

### Об'єктивні метрики стабільності (3 прогони)
| Техніка | Слабкий: унікальних відповідей із 3 | Сильний: унікальних із 3 |
|---------|:--:|:--:|
| DS2 (класифікація) | **3** (нестабільно) | **1** (`MIXED` щоразу) |
| SE3 (commit-меседж) | **3** (різні формати) | усі **однорядкові** `type(scope):` |

---

## Три крос-висновки, які дала саме перевірка (а не теорія)

1. **Якір першого символу > заборона обгортки.** `SE5` («start with `{`») дав голий JSON; `DS4` («no markdown») — ні, попри схожу мету. Це видно тільки на реальних прогонах.
2. **Сильний промпт регулює обсяг в обидва боки.** `SE1`/`SE5`/`DS2` — коротше; `DS3`/`SE2`/`SE4` — довше. Мета не «менше токенів», а «правильний обсяг під задачу».
3. **Не всі «класичні» техніки дають великий ефект на сучасній Haiku.** `SE6` (анти-галюцинації на очевидній пастці) майже не змінив поведінку — модель і так не вигадувала. Техніку треба перевіряти на *своїх* складних випадках, а не на хрестоматійних.

---

## Повторна перевірка 2026-09-19: що виправлено

Усі 14 файлів курсу ще раз звірено з першоджерелами: 60+ тверджень перевірено запитами до офіційних доксів, arXiv і блогів, числа ядра — із сирими JSON. **Ядро 1–8 (виміряні числа) помилок не має:** DS2, SE1, DS3, DS4, SE5, N=5 і Opus/Fable збіглися з `raw-outputs/`. Виправлено таке.

**Фактичні неточності (11):**
1. Ч.11: цитату Anthropic «consistently underperforms» **вигадав** дослідницький агент. У джерелі сказано інше: підагенти, розділені за ролями, витрачали більше токенів на координацію, ніж на роботу.
2. Ч.13: знак нібито змінювався в «MoE-гейтах». Насправді це вхід гейта SwiGLU у **щільних** 7–9B моделях.
3. Ч.13: «GGUF має CPU-шлях у vLLM» — за матрицею vLLM не має. CPU-шлях є в AWQ/GPTQ (x86) та INT8.
4. Ч.13: «EU AI Act повністю застосовний із серпня 2026» — застосовний з винятками; правила для високоризикових систем перенесено на 2.12.2027 і 2.08.2028.
5. Ч.10: 630 рядків у Karpathy — це код моделі й навчання, який правив агент, а не «оркестраційний Python». Приріст — мінус 11% часу навчання.
6. Ч.9: цитату DeepSeek про few-shot приписано картці моделі, а вона зі статті R1.
7. Ч.1, 4, 6, 7, README: prefill подано як «гарантований формат». Гарантію дають лише structured outputs (`output_config.format`), і вони є навіть на Haiku 4.5.
8. Ч.9, 13: «у локальних стеків немає схемного режиму» — Ollama (`format`) і llama.cpp (`json_schema`/`grammar`) його мають.
9. Ч.7: драбину позначено «виміряно на Haiku», хоча prefill ми не вимірювали.
10. Ч.9: «наш DS4/SE5» нібито використовували prefill — ні, там був якір у тексті промпта.
11. Ч.11: «3–15× на власній шкурі» — без одноагентного бейзлайна цей множник ми не виміряли.

**Застаріло після 2026-08-21 (5):** вийшов Claude Fable 5.1, а Fable 5 став Legacy (Ч.8); OpenAI рекомендує GPT-6 Astra (Ч.4, 9); вийшла Gemma 4 (Ч.13); читання кешу на Fable 5.1 коштує 0.025× (Ч.12); Python SDK 1.0+ не приймає `temperature`. Через останнє виправлення туторіалу Anthropic тепер займає два рядки (Ч.4), а кроки відтворення нижче фіксують `anthropic<1` (перевірено локально: SDK 1.7.0 дає `TypeError`, 0.125.0 працює).

**Текст:** 53 мовні порушення (транслітерації, ланцюги родового), неправильне слово в таблиці 12.3, граматика в 12.4, особисте звертання в 12.4. Джерела частин 9–13 стали посиланнями, у кожній частині зʼявився перелік перевірених джерел.

**Перевірено й підтверджено без змін** (вибірково): METR (16 розробників, 246 задач, −19%), DORA 2025, SpecBench, TDAD, SDD, NoLiMa, Chroma, EMNLP-2025, «selection bottleneck», Adversarial Review, Cognition 2025/2026, дослідницька система Anthropic (90.2%, 15×), 20 паралельних підагентів і 3 рівні вкладеності в Claude Code, Stop-hook із 8 блокуваннями, compaction (150K/50K), memory tool, кеш 0.1×/1.25×/2×, Qwen3.8-27B, DeepSeek V4, gpt-oss.

## Повторна перевірка 2026-09-24: що виправлено

Мовний лінтер отримав нові правила 23.09, а 22.09 вийшли нові моделі. Тому курс перевірено ще раз.

**Фактичні неточності (6):**
1. Ч.13: Willison цитувався неточно. Насправді він пише «wildly outperforming expectations» і наводить приклад із пеліканом. Порівняння з «моделями попереднього покоління» в нього немає.
2. Ч.13: MLX-кванти Qwen3.8 викладає спільнота mlx-community, а не сам Qwen. Твердження «сотні квантів у день випуску» ми не підтвердили й прибрали дату.
3. Ч.13: рядок «у локальних моделей немає structured outputs» суперечив виправленню від 19.09. Схемний режим в Ollama і llama.cpp є, але гарантує лише синтаксис.
4. Ч.8: у Haiku 4.5 «сирий chain-of-thought видно» — ні. Жодна модель не повертає сирі міркування, Haiku типово повертає підсумок.
5. Ч.8: `temperature` на Opus 4.8 і Fable дає 400 лише з нестандартним значенням, а не з будь-яким.
6. Ч.4: «гайд Willison не оновлювався з квітня 2026» перевірити не вдалося. Формулювання пом'якшено до того, що видно на сторінці.

**Застаріло після 19.09 (3):** 22.09 вийшов Claude Opus 5.5 ($4/$20, мислення завжди увімкнене, типовий effort `medium`), а Opus 5 став Legacy (Ч.4, 8, 9, 12, README). OpenAI додала GPT-6 Sol і Luna, які приймають `temperature` лише з `reasoning_effort: none` (Ч.9). DeepSeek 10.09 випустила V4.1-Flash (Ч.13).

**Мова:** 101 знахідка за новими правилами лінтера та 7 в архівному аудиті. Терміни тепер однакові в усьому курсі: bug → вада, review → рецензування, validation → перевірка, pipeline → конвеєр, default → типово. `AUDIT_REPORT.md` позначено як архівний знімок від 28.06.

**Перевірено й підтверджено без змін:** harness-пост Anthropic (`init.sh`, `claude-progress.txt`, аналогія зі змінами), пост про контекст-інжиніринг (визначення, Pokémon, 1–2 тис. токенів, JIT), цитата Karpathy у Willison, Managed Agents, gpt-oss, Gemma 3, Llama 4 як остання номерна Llama, ціна Sonnet 5, fallback і його платформи, вимога зберігати дані Fable 30 днів, дата, коли виведуть Haiku 4.5.

## Як відтворити

Скрипт workflow збережено тут (можна редагувати й перезапускати):
```
/Users/bibo/.claude/projects/-Users-bibo-----------------/6085e5a9-e68d-4d94-b342-029a5542f2fa/workflows/scripts/haiku-prompt-eng-validation-wf_c3699d75-208.js
```
Повний JSON усіх 30 відповідей + 16 сайтів:
```
/private/tmp/claude-501/-Users-bibo-----------------/6085e5a9-e68d-4d94-b342-029a5542f2fa/tasks/w5zg0ukbt.output
```
Друга партія (Haiku-vs-Sonnet, нові техніки, UK) — скрипт і повний JSON:
```
.../workflows/scripts/haiku-course-extensions-wf_a092010c-2f6.js
/private/tmp/claude-501/.../tasks/w67fx5rvi.output
```
Третя партія (надійність N=5, антипатерни, URL) — скрипт і повний JSON:
```
.../workflows/scripts/haiku-stability-antipatterns-wf_ad6c2be1-0db.js
/private/tmp/claude-501/.../tasks/wv5azqjjf.output
```

**Для строгого відтворення через raw-API** (рекомендовано для публікації):
1. Встанови ключ: `export ANTHROPIC_API_KEY=...` і `pip install "anthropic<1"`. Версія важлива: Python SDK 1.0+ не приймає `temperature` (`TypeError`, перевірено 2026-09-19), а кроки 2–3 його використовують.
2. Для кожної пари промптів виклич `messages.create(model="claude-haiku-4-5-...", max_tokens=1024, temperature=0)`.
3. Для тестів стабільності (`DS2`, `SE3`) зроби `temperature=0.7` і N=5 семплів, порахуй частку унікальних і коректних форматів.
4. Для `DS4`/`SE5` додай **prefill** (`messages=[..., {"role":"assistant","content":"{"}]`) і переконайся, що преамбула зникає.
